from django.db import models

# API APP - MODELS
# Owner: Colin (Backend Lead)
# Status: EMPTY - needs implementation
#
# Purpose: Define core API models if needed (may not be needed, use other apps)
#
# TODO:
# - Determine if this app needs models or if all models live in users/posts/friends
# - If needed, create shared models here (e.g., Notification, Activity)
# - Add __str__ methods for admin panel
# - Add Meta classes (ordering, verbose_name)
# - Create migrations: python manage.py makemigrations
# - Apply migrations: python manage.py migrate
#
# Create your models here.
